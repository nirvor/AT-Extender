# Example environment file for Docker Compose
# Copy this to .env and fill in your values

# Required: ALDI TALK credentials
RUFNUMMER=DeineRufnummer
PASSWORT=DeinPasswort

# Optional: Telegram notifications (set TELEGRAM=1 to enable)
TELEGRAM=0
BOT_TOKEN=DeinTelegramBotToken
CHAT_ID=DeineChatID

# Optional: Configuration
AUTO_UPDATE=1
SLEEP_MODE=smart
SLEEP_INTERVAL=70
BROWSER=chromium

# Note: You can also mount a config.json file instead of using environment variables
